# Local DOSBox conf (absolute mount paths)
config/dosbox.local.conf

# Games live wherever you keep them, never inside the repository.

# Local staging output and saved launcher settings
stage/
dgb-local.json

# OS / editor
.DS_Store
Thumbs.db
*~
*.swp
*.tmp
.reflex/
.vscode/

# Claude Code local state (machine-specific).
# .claude/settings.json is shared project config and IS tracked; everything
# else under .claude/ is per-machine and stays local.
.claude/*
!.claude/settings.json
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Stray dirs from shell test fixtures with odd names
(HEAD)/
(working)/

# Build scratch / staged CF image (regenerate with tools/make-media.sh)
dist/
media/
*.o
*.obj
*.map
